#####################################################
# Trivy Ignore File for go-rock/1.22-24.04         #
# Go 1.22 with Ubuntu 24.04 LTS (noble)            #
#####################################################

#####################################################
# The following vulnerabilities are NOT FIXED yet. #
# References: https://ubuntu.com/security/<CVE-ID>#
#####################################################

# Query race condition in crypto during Scan()
# Affects both Go 1.22 and 1.25
# CVSS: 7.0 (High) | Status: Needs evaluation
CVE-2025-47907

# tar.Reader unbounded memory allocation with sparse files
# Affects both Go 1.22 and 1.25
# CVSS: 4.3 (Medium) | Status: Needs evaluation
CVE-2025-58183

# HTTP cookie parsing unbounded memory consumption
# Affects both Go 1.22 and 1.25
# CVSS: 5.3 (Medium) | Status: Needs evaluation
CVE-2025-58186

# Name constraint checking quadratic algorithm in crypto/x509
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2025-58187

# HostnameError quadratic string construction
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2025-61729

# TLS session resumption bypass in crypto/tls
# Affects both Go 1.22 and 1.25
# CVSS: 9.1 (Critical) | Status: Vulnerable (not yet fixed)
CVE-2025-68121

# net/url unbounded memory consumption with URL forms
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2025-61726

# archive/zip super-linear indexing algorithm DoS
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 6.5 (Medium) | Status: Needs evaluation
CVE-2025-61728

# net.Is methods (IsPrivate etc.) incorrect behavior for IPv6
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 9.8 (Critical) | Status: Needs evaluation
CVE-2024-24790

# net/http request smuggling via bare LF as line terminator
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 9.1 (Critical) | Status: Needs evaluation
CVE-2025-22871

# PATH executable injection via os/exec
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 6.5 (Medium) | Status: Needs evaluation
CVE-2025-47906

# net/netip Parse allows non-IPv6 zone IDs in IPv4 values
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 5.3 (Medium) | Status: Needs evaluation
CVE-2025-47912

# crypto/x509 malicious DER payload causes large memory allocation
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 5.3 (Medium) | Status: Needs evaluation
CVE-2025-58185

# crypto/x509 DSA public key chain validation DoS
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2025-58188

# crypto/tls ALPN error leaks hostname
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 5.3 (Medium) | Status: Needs evaluation
CVE-2025-58189

# Parsing invalid inputs causes non-linear processing time
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2025-61723

# net/textproto Reader.ReadResponse quadratic string handling
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 5.3 (Medium) | Status: Needs evaluation
CVE-2025-61724

# net/mail ParseAddress domain-literal quadratic processing
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2025-61725

# crypto/x509 excluded subdomain constraint bypass
# Go 1.22 specific (not in Ubuntu 26.04)
# CVSS: 6.5 (Medium) | Status: Needs evaluation
CVE-2025-61727

# net/url incorrect IPv6 host literal parsing
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-25679

# crypto/x509 + crypto/tls certificate validation DoS
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-32280

# crypto/x509 inefficient certificate verification DoS
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-32281

# crypto/tls multiple TLS 1.3 key updates cause DoS
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-32283

# net LookupCNAME crash on long CNAME chain via cgo
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-33811

# net/http HTTP/2 SETTINGS frame causes infinite loop
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-33814

# net/mail ParseAddress/ParseAddressList crafted input DoS
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-39820

# net/url CVE-2026-27142 bypass via crafted URL
# Affects both Go 1.22 and 1.25
# CVSS: 6.1 (Medium) | Status: Needs evaluation
CVE-2026-39823

# net/http/httputil ReverseProxy unintended parameter forwarding
# Affects both Go 1.22 and 1.25
# CVSS: 5.3 (Medium) | Status: Needs evaluation
CVE-2026-39825

# html/template script tag with empty type attribute incorrect escaping
# Affects both Go 1.22 and 1.25
# CVSS: 6.1 (Medium) | Status: Needs evaluation
CVE-2026-39826

# net Dial and LookupPort panic on NUL input on Windows
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-39836

# net/mail DoS via consumePhrase on pathological RFC 5322 input
# Affects both Go 1.22 and 1.25
# CVSS: 7.5 (High) | Status: Needs evaluation
CVE-2026-42499
